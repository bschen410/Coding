#include <bits/stdc++.h>
#define ouo ios_base::sync_with_stdio(false), cin.tie(0)
using namespace std;

int main() {
    ouo;
    int n, k;
    cin >> n >> k;
    vector<pair<int, int> > time(n);
    for (int s = 0; s < n; s++)
        cin >> time[s].first;
    for (int s = 0; s < n; s++)
        cin >> time[s].second;
    //
    int dp[k+1];
    for (int)
    return 0;
}
