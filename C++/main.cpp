#include <bits/stdc++.h>
#define ouo ios_base::sync_with_stdio(false), cin.tie(0)
#define ll long long
#define db double
using namespace std;

int main() {
    int arr[3] = {'a', 'b', 'c'};
    for (int i = 0; i < 3; i++) {
        if (arr[i] == 'a') {
            cout << "there is 'a' in array" << endl;
        }
    }
}
